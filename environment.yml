name: elysium
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pyyaml>=6.0
  - numpy>=1.26
  - pip:
    - pydantic>=2.0
    - python-dotenv>=1.0
    - openai>=1.30.0
    - chromadb>=0.5.0
    - sentence-transformers>=3.0.0
    - loguru>=0.7.0
    - pytest>=8.0
